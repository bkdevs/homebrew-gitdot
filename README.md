# homebrew-gitdot

Homebrew tap for [gitdot](https://gitdot.io).

## Install

```sh
brew tap bkdevs/gitdot
brew install gitdot-cli
```

Then run:

```sh
dot --help
```

## Formulae

- `gitdot-cli` — the command-line interface for gitdot.
